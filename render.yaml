services:
  - type: web
    name: heading-tag-analyser
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run heading_analyzer_app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true
    envVars:
      - key: APP_PASSWORD
        sync: false
